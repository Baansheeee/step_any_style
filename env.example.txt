# ============================================
# Step & Style - Environment Variables Template
# ============================================
# Copy this file to .env.local and fill in your actual values
# DO NOT commit .env.local to version control
# 
# To use: Rename this file to .env.example or copy contents to .env.local

# ============================================
# Application Settings
# ============================================
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# Database Configuration
# ============================================
# Choose ONE database option below and comment out the others

# --- Option 1: MongoDB (Mongoose) ---
# DATABASE_TYPE=mongodb
# MONGODB_URI=mongodb://localhost:27017/iplstore
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/iplstore?retryWrites=true&w=majority

# --- Option 2: PostgreSQL (Prisma/Postgres.js) ---
DATABASE_TYPE=postgresql
DATABASE_URL=postgresql://username:password@localhost:5432/iplstore?schema=public
# For production with SSL:
# DATABASE_URL=postgresql://username:password@host:5432/iplstore?schema=public&sslmode=require

# --- Option 3: MySQL (Prisma/MySQL2) ---
# DATABASE_TYPE=mysql
# DATABASE_URL=mysql://username:password@localhost:3306/iplstore

# --- Option 4: SQLite (Prisma) - For development only ---
# DATABASE_TYPE=sqlite
# DATABASE_URL=file:./dev.db

# ============================================
# Authentication & Security
# ============================================
# Generate a secure random string for JWT secret
# You can use: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Session/Cookie settings
SESSION_SECRET=your-session-secret-key-change-this-in-production
ADMIN_REGISTRATION_KEY=change-this-admin-key
COOKIE_SECURE=false
COOKIE_SAME_SITE=strict

# ============================================
# API Keys & External Services
# ============================================
# Email service (using Gmail SMTP by default)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=stepandstyle@gmail.com
SMTP_PASS=rnxkwkkzofhvcscp
SMTP_FROM="Step & Style <stepandstyle@gmail.com>"

# Payment gateway (e.g., Stripe, PayPal)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# File upload service (Cloudinary — unsigned upload preset)
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your-cloudinary-cloud-name
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=your-upload-preset

# AWS S3 (if using for file storage)
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_REGION=us-east-1
# AWS_S3_BUCKET_NAME=iplstore-uploads

# ============================================
# Admin & Security
# ============================================
# Default admin credentials (change after first login)
ADMIN_EMAIL=admin@iplstore.com
ADMIN_PASSWORD=admin123

# ============================================
# Feature Flags
# ============================================
# NEXT_PUBLIC_ENABLE_ANALYTICS=false
# NEXT_PUBLIC_ENABLE_CHAT=false

# ============================================
# Development & Debugging
# ============================================
# Enable detailed error logging in development
DEBUG=false

# ============================================
# Production Settings
# ============================================
# For production, set these:
# NODE_ENV=production
# NEXT_PUBLIC_APP_URL=https://yourdomain.com
# COOKIE_SECURE=true
# DATABASE_URL=your-production-database-url

